{
  "name": "@volta-oss/data-clean-room-sheets",
  "version": "3.0.0",
  "private": false,
  "type": "module",
  "description": "Spreadsheet templates for cleaning small-business data before any automation or analysis.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "npm run build && node --check src/*.js && node --check test/*.js",
    "test": "npm run build && node --test test/*.test.js",
    "start": "python3 -m http.server 4173",
    "validate": "npm test",
    "certify": "npm run build && node --test test/v3.test.js",
    "test:all": "npm test",
    "test:python": "PYTHONPATH=backend/python python3 -m unittest discover -s backend/python/tests",
    "test:backend": "npm run test:python",
    "clean": "rm -f src/*.js src/*.js.map src/*.d.ts test/*.js test/*.js.map test/*.d.ts",
    "ci": "npm ci && npm run lint && npm test && npm run test:backend --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "docker:build": "docker build -t volta-oss/data-clean-room-sheets:local .",
    "docker:run": "docker compose up --build",
    "serve:python": "PYTHONPATH=backend/python python3 -m data_clean_room_sheets_backend.server"
  },
  "keywords": [
    "ai-automation",
    "digital-equity",
    "local-first",
    "oss",
    "release",
    "volta"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volta-npo/data-clean-room-sheets.git"
  },
  "homepage": "https://volta-npo.github.io/data-clean-room-sheets/",
  "bugs": {
    "url": "https://github.com/volta-npo/data-clean-room-sheets/issues"
  },
  "author": "Volta Open Source <volta.npo@gmail.com>",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "@types/node": "^25.9.1"
  }
}
